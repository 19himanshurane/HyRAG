# Only what the code uses today; each step adds its own dependencies.
beautifulsoup4>=4.12
pdfplumber>=0.11
numpy>=1.26
httpx>=0.27
python-dotenv>=1.0
chromadb>=1.5       # vector store (Apache-2.0)
rank-bm25>=0.2.2    # keyword scoring (Apache-2.0)

# dev: generates the sample PDF and test PDFs; runs the test suite
fpdf2>=2.7
pytest>=8

# For exact, reproducible versions (parsing output and eval numbers depend on them):
#   pip install -r requirements-lock.txt
